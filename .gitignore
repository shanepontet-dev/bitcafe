# OS cruft
.DS_Store
**/.DS_Store

# large local-only media: never committed (GitHub hard-rejects >100MB files
# anyway, and these two are real movie files pending a decision on how/if
# movie-night.html gets real video sources in the public deploy)
media/*.mp4

# local tooling (wrangler's local state) -- never committed
node_modules/
.wrangler/

# package.json/package-lock.json ARE committed now: the /admin backend's
# Cloudflare Pages Functions have one real dependency (aws4fetch, for
# signing R2 presigned upload URLs). the public site itself still has no
# build step -- this only feeds Cloudflare's Functions bundler.
